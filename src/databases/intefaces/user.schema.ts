import mongoose from 'mongoose';
const { Schema } = mongoose;

/*const userSchema = new Schema({
  ProfileImage:  String, // String is shorthand for {type: String}
  Email: String,
  Name:   String,
  Password: [{ key: String, characters: String }],
  Date: { type: Date, default: Date.now },
  Admin: Boolean,
  Biography: String,
});*/

